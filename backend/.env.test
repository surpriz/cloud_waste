# Test Environment Configuration
# This file is used for pytest tests only

APP_NAME=CloudWaste
APP_ENV=test
DEBUG=false
SECRET_KEY=test-secret-key-for-pytest-only
API_V1_PREFIX=/api/v1

# Security - Test Keys (DO NOT use in production!)
ENCRYPTION_KEY=_blQFBhp9rN2MiDCejvwmVLZDx7G0rd_nhUN_ab9qlk=
JWT_SECRET_KEY=test-jwt-secret-key-for-pytest
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
REFRESH_TOKEN_REMEMBER_ME_EXPIRE_DAYS=30

# Database - SQLite in-memory (configured in conftest.py)
DATABASE_URL=sqlite+aiosqlite:///:memory:

# Redis - Mock for tests (no real Redis needed)
REDIS_URL=redis://localhost:6379/15

# Celery - Disabled for tests
CELERY_BROKER_URL=redis://localhost:6379/15
CELERY_RESULT_BACKEND=redis://localhost:6379/15

# CORS - Test configuration
# Note: ALLOWED_ORIGINS can be either JSON array or comma-separated string
ALLOWED_ORIGINS=["http://localhost:3000", "http://testserver"]
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=600

# Email - Mock for tests (no real SMTP needed)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=test
SMTP_PASSWORD=test
EMAILS_FROM_EMAIL=test@cloudwaste.com
EMAILS_FROM_NAME=CloudWaste Test

# Email Verification
EMAIL_VERIFICATION_TOKEN_EXPIRE_HOURS=168
UNVERIFIED_ACCOUNT_CLEANUP_DAYS=14
FRONTEND_URL=http://localhost:3000

# AWS - Mock credentials for tests
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_DEFAULT_REGION=us-east-1

# AWS SES - Mock for tests
AWS_SES_REGION=us-east-1
AWS_SES_ACCESS_KEY_ID=test_ses_key
AWS_SES_SECRET_ACCESS_KEY=test_ses_secret

# AI Assistant - Mock for tests
ANTHROPIC_API_KEY=test-anthropic-api-key
CHAT_MAX_MESSAGES_PER_USER_PER_DAY=50
CHAT_CONTEXT_MAX_RESOURCES=20
CHAT_MODEL=claude-haiku-4-5-20250818

# Rate Limiting - Disabled for most tests
RATE_LIMIT_ENABLED=false
RATE_LIMIT_AUTH_LOGIN=1000/minute
RATE_LIMIT_AUTH_REGISTER=1000/minute
RATE_LIMIT_AUTH_REFRESH=1000/minute
RATE_LIMIT_SCANS=1000/minute
RATE_LIMIT_ADMIN=1000/minute
RATE_LIMIT_API_DEFAULT=1000/minute
